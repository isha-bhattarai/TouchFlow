{
  "name": "touchflow",
  "version": "0.1.0",
  "private": true,
  "description": "Transform your smartphone into a wireless touchpad, keyboard and productivity remote for your computer.",
  "workspaces": ["packages/*", "apps/*"],
  "scripts": {
    "test": "npm run test --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "build": "npm run build --workspaces --if-present"
  },
  "engines": { "node": ">=20" },
  "license": "MIT"
}
